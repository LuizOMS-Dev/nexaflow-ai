# RAG

Estado atual:

- KnowledgeDoc + chunks (chunking no import)
- Recuperação: docs published (take N) no prompt
- Embeddings pgvector: schema preparado (comentário), **não** similarity search em produção nesta entrega

Próximo passo seguro: top-N chunks por keyword overlap, depois pgvector.
